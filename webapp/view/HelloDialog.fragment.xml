<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Dialog
        id="helloDialog"
        title="{i18n>successDialogTitle}"
    >
        <FormattedText
            htmlText="{i18n>successMessage}"
            class="sapUiSmallMargin sapThemeHighlight-asColor"
        />
        <beginButton>
            <Button
                text="{i18n>dialogCloseButtonText}"
                press=".onCloseDialog"
                type="Emphasized"
            />
        </beginButton>
    </Dialog>
</core:FragmentDefinition>
